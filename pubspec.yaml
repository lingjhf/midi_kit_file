name: midi_kit_file
description: Pure Dart Standard MIDI File models, readers, writers, and timing utilities.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.5

dev_dependencies:
  coverage: ^1.15.0
  lints: ^6.0.0
  test: ^1.26.3
